{
  "name": "crystal-cascade-paystack-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side Paystack transaction initialize/verify for Crystal Cascade's sideload IAP fallback. Keeps the Paystack secret key off the client.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
